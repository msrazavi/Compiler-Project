/* sample 10 */

int add_sub(int a, int b, int add) {
    if (add == 1) {
        return a + b;
    } else {
        return a - b;
    } endif
}

int main(void)
{
    output(add_sub(5, 2, 1));
    output(add_sub(5, 2, 0));
    output(add_sub(5, 1));
    output(add_sub(5, 1, 1, 1));
}

